# Preserved Unimplemented Content

These are the still-unimplemented forward-looking entries from docs 01–29, preserved intact rather
than promoted into the current rules or lost during the spec/data split. Source wording is retained
where a concrete entry existed. This file is design provenance, not a live task list: activation,
priority, decomposition, and status are tracked in deferred Beads epic `vibes_and_magic-1zo`.

## Combat leverage direction (doc 03)

Beads: `vibes_and_magic-1zo.1` (deferred).

> Leverage effects strong vs neutrals, damped vs enemy heroes (shorter durations / level-scaled resistance).

No current general hero-resistance system implements this direction. It remains a future design
candidate, subordinate to target scaling and the behavior-not-numbers filter.

## Cartwright’s Wheel (doc 13 pencil)

Beads: `vibes_and_magic-1zo.2` (deferred).

> Cartwright's Wheel (+1000 move, breaks a random item? no — behavior without downside dice: +1000 move, single use)

It was not included in the completed consumable catalog. Preserve the no-random-downside ruling if
it is introduced later.

## Tier-seven and reserve creatures (docs 05, 14, 21)

Beads: `vibes_and_magic-1zo.3` (deferred).

> (Hearth-Hound and Dexter Lion → tier-7/neutral-dwelling pool, later.)

> (Jack-in-the-Box, Music Box → tier-7/neutral pool.)

> (The Stampede, the Cortège → reserved for tier 7.)

Hearth-Hound has since entered the neutral pool; the remaining named reserve creatures have no
current unit definitions. Tier 7 as a castle tier remains deferred.

## Expansion milestone deferrals (doc 21)

Beads: `vibes_and_magic-1zo.4` (deferred umbrella; decompose only when promoted).

> Tier 7 units, the Seamborn as a recruitable faction, campaigns/story, map editor, random map generation, full siege systems, multiplayer beyond hot-seat, art beyond SVG tokens, sound.

These remain out of current scope. The Seamborn ID and encounters are reserved, but no recruitable
faction/castle catalog exists.

## Water deferrals (doc 26)

Beads: `vibes_and_magic-1zo.5` (deferred umbrella; decompose only when promoted).

> Ship-to-ship boarding minigames, ship upgrades, sea omens, the Driftfolk as a faction (the sea creatures here are their advance scouts, canonically), underwater layer. Log, don't build.

These remain absent from the current rules and catalogs.
